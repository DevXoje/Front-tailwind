import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
/**
 * CartService
 * this service is responsible for managing the cart, adding and removing items from the cart, storing the cart in the local storage, and calculating the total price of the cart.
 */
export class CartService {
  constructor() {}
}
